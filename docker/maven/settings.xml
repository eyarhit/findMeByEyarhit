<?xml version="1.0" encoding="UTF-8"?>
<settings xmlns="http://maven.apache.org/SETTINGS/1.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.2.0 https://maven.apache.org/xsd/settings-1.2.0.xsd">
  <!-- Retries for unstable/slow downloads (Docker build on weak networks). -->
  <profiles>
    <profile>
      <id>findme-http-retry</id>
      <properties>
        <aether.connector.http.connectionMaxTries>10</aether.connector.http.connectionMaxTries>
        <aether.connector.https.connectionMaxTries>10</aether.connector.https.connectionMaxTries>
        <maven.wagon.http.retryHandler.count>10</maven.wagon.http.retryHandler.count>
        <maven.wagon.httpconnectionManager.ttlSeconds>120</maven.wagon.httpconnectionManager.ttlSeconds>
      </properties>
    </profile>
  </profiles>
  <activeProfiles>
    <activeProfile>findme-http-retry</activeProfile>
  </activeProfiles>
</settings>
