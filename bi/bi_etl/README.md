# ⚠️ Déprécié — utiliser `bi/talend/`

L’ETL officiel PFE est **Talend** :

- Conception : `bi/talend/studio/FindMe_Load_DW/`
- Runtime Docker : `bi/talend/docker/` → service `talend-etl`

```cmd
docker compose run --rm talend-etl
```

Ce dossier (`bi_etl`) est conservé comme référence technique uniquement.
