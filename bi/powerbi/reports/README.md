# Rapports Power BI locaux

Placez ici **`FindMe_BI_Auto.pbix`** (cree une fois via `starter/CREER_PBIX.md`).

Ce fichier **n'est pas versionne** dans Git (evite les conflits binaires).

`GIT_PULL_BI.cmd` ne supprime plus ce dossier (contrairement a l'ancien `git clean`).

Pour le modele 3 pages : utilisez `FindMe-Dashboard.pbip` a la racine de `bi/powerbi/FindMe-Dashboard/`.
